# F2 recovery 1: interrupted control, unchanged configuration

2026-09-10, after 36 successful new F2 runs. Tuning selection is already frozen (SHA-256 b106d53b0109e0ae88745539510f1a178c3dea8513cb612e9d1203877f1e0232). All protocol settings, selection rules, populations, seeds, source files and successful artifacts remain unchanged.

The first CIC primary centralized seed-29 control logged three epochs before an interruption. The supervisor recorded exit code 15, a wall-time resource stop, 2,387.0306483 elapsed seconds and 3,160,498,176 bytes peak sampled process-tree RSS. No final model/probabilities/metrics were produced. Windows System events record a critical-battery trigger at 14:49:51 local time and entering sleep at 14:49:53; a later power-source event occurred at 15:29:19. At investigation, GetSystemPowerStatus reported AC connected and 10% battery. These events support a host-power interruption; no model-score failure or invalid numerical output was observed. The sleeping/unscheduled supervisor could enforce its time cap only after execution resumed, so elapsed time exceeds the nominal 600-second cap.

Preserve the failed JSON, log and partial model directory under explicit attempt-1 names. The recovery manifest binds their exact bytes and records original-to-archive paths; the JSON itself is not rewritten. Keep the failed JSON in the runs directory so the original supervisor continues charging its entire elapsed time. Retry only the same control from its initial weights; no checkpoint continuation or retuning. The failed attempt is not a completed method cell and receives no score. Record it separately in the global run/failure ledger.

The cumulative 14,400-second EXP-004 budget remains unchanged and includes the full failed-attempt charge. The 600-second reservation and 12-GiB process-tree/4-GiB allocator caps remain unchanged. The original driver resumes the fixed schedule, rechecking and reusing successful records. Source hashes must remain identical to preregistration commit 5245063 even if recovery documentation creates a later Git commit. Reaching the budget still stops the experiment explicitly; this recovery does not grant extra compute.
