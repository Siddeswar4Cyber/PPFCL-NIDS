# Preserve source bytes for the script hashes recorded in audit manifests.
scripts/*.py -text
reports/audit/attempts/*.py -text
docs/project-brief.txt -text

# B2 selection and feature-spec bindings hash exact report bytes.
reports/baselines/runs/*.json -text
reports/cp6/**/*.json -text
